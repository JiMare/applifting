import { ReactElement } from "react";
import { Box, Button, Typography, Container } from "@mui/material";

export const ArticleCardDetail = (): ReactElement => {
  return <Container maxWidth="xl">ArticleCardDetail</Container>;
};
